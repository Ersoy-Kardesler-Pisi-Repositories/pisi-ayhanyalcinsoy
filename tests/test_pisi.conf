[general]
distribution = Pardus
distribution_id = pardus
architecture = x86_64
hostname = test.local

[build]
jobs = 1
debug = False

[directories]
cache_dir = /tmp/pisi-test-cache
work_dir = /tmp/pisi-test-work
install_dir = /tmp/pisi-test-install

[repos]
pardus-2007 = pardus-2007
contrib-2007 = contrib-2007 